pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
        maven {
            name = 'Creeperhost'
            url = 'https://maven.creeperhost.net/'
        }
    }
}

include("common", "fabric", "forge")